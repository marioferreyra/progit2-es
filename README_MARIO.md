COMANDOS PARA OBTENER EL LIBRO EN FORMATO PDF

* EJECUTAR ESTOS COMANDOS DENTRO DEL DIRECTORIO QUE CONTIENE EL PROYECTO

$ sudo apt install ruby-bundler

$ bundle update

$ bundle install

$ bundle exec rake book:build

